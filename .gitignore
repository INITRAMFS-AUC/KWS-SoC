# this is an executable
kws_soc_tb

# build directories and output
obj_dir/
build/
build_verilator/
yosys_build/
quartus_work_dir/
quartus_src_dir/
build-kws-soc/
CycloneIV/

# Generated PLL (produced by scripts/gen_pll.py, regenerated via `make gen_pll`)
quartus/ip/clock_pll_gen/
verilator_sources.f
*.elf
*.o
*.dis
*.qsf
*.qpf

# development stuff
tags
.zed
.vscode
quartus/c5_pin_model_dump.txt
soc_conf.sh
# TODO: Add real template sh
soc_conf_temp.sh

# Simulation waveforms
*.vcd
*.fst
*.gtkw

# Sim / debug capture logs left at the project root by ad-hoc test runs
# (mel_*.log, infer.log, sim.log, uart.log, ring_dump.hex, etc.).
# Anchored to the root so we don't accidentally hide intentional .log /
# .hex files inside test/ or sim/.
/*.log
/ring_dump.hex
/testres.hex

# Personal notes
notes.md
images
hazard3_instantiation_params.vh
*.saif

# Nix
.direnv/
result
result-*
.envrc

# Python 
__pycache__/
*.pyc
*.pyo
scripts/venv/
venv/

# GCC dependency files (may appear outside build/ during out-of-tree invocations)
*.d


# data
sim/playback/
